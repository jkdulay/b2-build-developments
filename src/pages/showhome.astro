---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import SectionHeading from '../components/SectionHeading.astro';
import Lightbox from '../components/Lightbox.astro';
import { LayoutGrid, Star, BedDouble, Bath, Home } from '@lucide/astro';
import { getProjectBySlug } from '../data/projects';

const brooklyn = getProjectBySlug('brooklyn');
// Featured photos for the showhome 3-up gallery (open in lightbox)
const featurePhotos = [
  { src: '/photos/brooklyn/front-exterior-day.jpg',  alt: 'The Brooklyn — front exterior',         caption: 'Front Exterior' },
  { src: '/photos/brooklyn/kitchen-cook-area.jpg',  alt: "The Brooklyn — chef's kitchen",         caption: "Chef's Kitchen" },
  { src: '/photos/brooklyn/wine-cellar.jpg',        alt: 'The Brooklyn — glass wine display',     caption: 'Glass Wine Display' },
];
---

<Layout
  title="The Brooklyn — B2 Build Ltd | 2220 8 Street NE, Calgary"
  description="The Brooklyn by B2 Build Ltd — a refined contemporary residence at 2220 8 Street NE, Calgary. Thoughtful design, premium finishes, and modern inner-city living."
>
  <Header />

  <!-- PAGE HERO with real Brooklyn front exterior (per spec: "Select hero page Video slide").
       Video URL still TBD; poster image is the real Brooklyn front exterior at dusk. -->
  <!-- TODO: Replace poster with The Brooklyn hero video when supplied. -->
  <section class="relative h-[70vh] min-h-[480px] bg-black flex items-end overflow-hidden">
    <img
      src="/photos/brooklyn/front-exterior-dusk.jpg"
      alt="The Brooklyn — front exterior at dusk"
      class="absolute inset-0 w-full h-full object-cover opacity-80"
    />
    <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-black/20 to-transparent"></div>
    <div class="relative z-10 max-w-7xl mx-auto px-6 lg:px-12 pb-12 w-full">
      <p class="text-gold text-xs tracking-[0.4em] uppercase mb-3">FEATURE HOME</p>
      <h1 class="font-display text-4xl lg:text-6xl text-white">
        THE BROOKLYN
      </h1>
      <p class="text-white/70 text-sm mt-2 uppercase tracking-[0.2em]">2220 8 Street NE &nbsp;|&nbsp; Calgary, Alberta &nbsp;|&nbsp; 2026</p>
    </div>
  </section>

  <!-- GALLERY: 1 large + 2 stacked (per spec: "Switch placement of Butler Crescent front photo and the kitchen photo. Keep the hero photo.")
       Real Brooklyn photos: front exterior large, kitchen + wine display stacked. -->
  <section class="py-16 bg-white">
    <div class="max-w-7xl mx-auto px-6 lg:px-12">
      <div class="grid lg:grid-cols-3 gap-4 h-auto lg:h-[540px]">
        <!-- Large hero left (front exterior) -->
        <div class="lg:col-span-2 h-64 lg:h-full overflow-hidden">
          <button
            type="button"
            class="block w-full h-full cursor-pointer focus:outline-none focus:ring-2 focus:ring-gold"
            aria-label="Open image 1: The Brooklyn front exterior"
            onclick="document.getElementById('showhome-lb').openLightbox(0)"
          >
            <img
              src={featurePhotos[0].src}
              alt={featurePhotos[0].alt}
              class="w-full h-full object-cover hover:scale-105 transition-transform duration-700"
            />
          </button>
        </div>
        <!-- Two stacked right -->
        <div class="grid grid-rows-2 gap-4 h-64 lg:h-full">
          <div class="overflow-hidden">
            <button
              type="button"
              class="block w-full h-full cursor-pointer focus:outline-none focus:ring-2 focus:ring-gold"
              aria-label="Open image 2: The Brooklyn chef's kitchen"
              onclick="document.getElementById('showhome-lb').openLightbox(1)"
            >
              <img
                src={featurePhotos[1].src}
                alt={featurePhotos[1].alt}
                class="w-full h-full object-cover hover:scale-105 transition-transform duration-700"
              />
            </button>
          </div>
          <div class="overflow-hidden">
            <button
              type="button"
              class="block w-full h-full cursor-pointer focus:outline-none focus:ring-2 focus:ring-gold"
              aria-label="Open image 3: The Brooklyn glass wine display"
              onclick="document.getElementById('showhome-lb').openLightbox(2)"
            >
              <img
                src={featurePhotos[2].src}
                alt={featurePhotos[2].alt}
                class="w-full h-full object-cover hover:scale-105 transition-transform duration-700"
              />
            </button>
          </div>
        </div>
      </div>

      <!-- Full Brooklyn gallery (hidden, opened from lightbox when user navigates past the 3 featured photos) -->
      <!-- The lightbox itself uses the FULL project gallery as its photo source. -->
    </div>
  </section>

  <!-- SPECS: 5-icon row -->
  <section class="py-16 bg-white border-y border-black/10">
    <div class="max-w-5xl mx-auto px-6 lg:px-12">
      <SectionHeading
        eyebrow="HIGHLIGHTS"
        title="What Makes This Home Special"
        centered
      />
      <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-8 mt-10">
        <div class="text-center">
          <div class="w-14 h-14 mx-auto mb-4 bg-white border border-black/10 flex items-center justify-center">
            <LayoutGrid class="w-6 h-6 text-gold" />
          </div>
          <p class="text-black text-xs tracking-[0.15em] uppercase font-medium">Modern Open Concept</p>
        </div>
        <div class="text-center">
          <div class="w-14 h-14 mx-auto mb-4 bg-white border border-black/10 flex items-center justify-center">
            <Star class="w-6 h-6 text-gold" />
          </div>
          <p class="text-black text-xs tracking-[0.15em] uppercase font-medium">Elevated Finishes</p>
        </div>
        <div class="text-center">
          <div class="w-14 h-14 mx-auto mb-4 bg-white border border-black/10 flex items-center justify-center">
            <BedDouble class="w-6 h-6 text-gold" />
          </div>
          <p class="text-black text-xs tracking-[0.15em] uppercase font-medium">Spacious Bedrooms</p>
        </div>
        <div class="text-center">
          <div class="w-14 h-14 mx-auto mb-4 bg-white border border-black/10 flex items-center justify-center">
            <Bath class="w-6 h-6 text-gold" />
          </div>
          <p class="text-black text-xs tracking-[0.15em] uppercase font-medium">Luxurious Ensuites</p>
        </div>
        <div class="text-center col-span-2 sm:col-span-1">
          <div class="w-14 h-14 mx-auto mb-4 bg-white border border-black/10 flex items-center justify-center">
            <Home class="w-6 h-6 text-gold" />
          </div>
          <p class="text-black text-xs tracking-[0.15em] uppercase font-medium">Suite Potential</p>
        </div>
      </div>
    </div>
  </section>

  <!-- PROJECT DESCRIPTION -->
  <section class="py-16 bg-white">
    <div class="max-w-3xl mx-auto px-6 lg:px-12">
      <SectionHeading
        eyebrow="CUSTOM BUILD"
        title="The Brooklyn"
        subtitle="2220 8 Street NE · Calgary, Alberta · 2026"
        centered
      />
      <div class="space-y-5 text-black/75 text-base leading-relaxed">
        {brooklyn?.longDescription.map((para) => (
          <p>{para}</p>
        ))}
      </div>
    </div>
  </section>

  <!-- LINK TO FULL PROJECT PAGE -->
  <section class="py-12 bg-white border-t border-black/10">
    <div class="max-w-3xl mx-auto px-6 text-center">
      <a href="/projects/brooklyn" class="btn-outline-black inline-flex">
        VIEW FULL PROJECT DETAILS
      </a>
    </div>
  </section>

  <!-- CTA -->
  <section class="py-20 bg-black">
    <div class="max-w-3xl mx-auto px-6 text-center">
      <p class="text-gold text-xs tracking-[0.4em] uppercase mb-4">SCHEDULE A VIEWING</p>
      <h2 class="font-display text-3xl lg:text-4xl text-white mb-4">
        Ready to View The Brooklyn?
      </h2>
      <p class="text-white/60 text-sm mb-10 max-w-lg mx-auto">
        Reach out to schedule a private tour of our latest custom build in Winston Heights.
      </p>
      <a href="tel:+14036192702" class="btn-gold text-base px-8 py-4">
        CALL (403) 619-2702
      </a>
    </div>
  </section>

  <!-- LIGHTBOX: opens from the 3 featured thumbs above. Spans only the same 3 photos
       (front exterior, chef's kitchen, glass wine display). For the full 14-photo
       Brooklyn gallery, click "View Full Project Details" below. -->
  <Lightbox id="showhome-lb" photos={featurePhotos} />

  <Footer />
</Layout>
